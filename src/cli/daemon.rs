use std::{
    collections::LinkedList,
    error,
    fmt,
    sync::{
        Arc,
        mpsc::{self, Receiver, Sender, SyncSender},
    },
    thread,
};

pub fn start() {
    //TODO(shane)
}

pub fn daemon_running() -> bool {
    //TODO(shane)
    false
}

pub fn forward_command(cmd: &str) {
    //TODO(shane)
}

fn receive_command() {
    //TODO(shane)
}

#[cfg(test)]
mod tests {
    use super::*;
    //TODO(shane) write tests
}
